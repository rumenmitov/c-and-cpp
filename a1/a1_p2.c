/* 
   CH-230-A 
   a1_p2.c
   Rumen Mitov
   rmitov@jacobs-university.de 
*/

#include <stdio.h>

int main() {
    int result; /* The result of our calculation */
    result = (2 + 7) * 9 / 3;
    printf("The result is %d\n", result);
    return 0;
}

/*
 * The function printf was missing the argument that would have 
 * displayed the result.
*/
